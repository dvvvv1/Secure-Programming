
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h> /* for fork */
#include <sys/types.h> /* for pid_t */
#include <sys/wait.h> /* for wait */
#include <iostream>
#include <unistd.h> 
#include <string.h>

using namespace std;

int main(void)
{
  int i = 1;
  string input1 = "zzuf -r 0.01 -s < \"example.png\" > " + "\"" + i+ "1-example.png\"";
  cout <<input1;
    //const char* input = "zzuf -r 0.01 -s 1 < \"example.png\" > " + "\"exmample"+i+".png\"";
    //cout << input;
    //int sys = system(input);
    // cout <<endl;
    //cout <<sys;
    // cout <<endl;
 
  return 0;
}
